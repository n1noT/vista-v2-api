DATABASE_URL="postgresql://vista:vista@localhost:5432/vista_dev?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="7d"
FRONTEND_URL="http://localhost:4200"
PORT=3000
# Get a free key at https://www.football-data.org/client/register (10 req/min limit).
# Without it, the football-data sync cron logs a warning and skips instead of failing to boot.
FOOTBALL_DATA_API_KEY=""
FOOTBALL_DATA_BASE_URL="https://api.football-data.org/v4"
