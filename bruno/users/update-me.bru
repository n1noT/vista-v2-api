meta {
  name: Update me
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/users/me
  body: json
  auth: inherit
}

body:json {
  {
    "pseudo": "updatedpseudo"
  }
}

docs {
  Requires the access_token cookie. 409 if the new pseudo is already taken.
}
